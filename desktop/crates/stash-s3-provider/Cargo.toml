[package]
name = "stash-s3-provider"
version = "0.1.0"
edition = "2021"

[dependencies]
stash-core = { path = "../stash-core" }
sha2 = "0.10"
ureq = { version = "2", default-features = false, features = ["tls"] }

[dev-dependencies]
tiny_http = "0.12"
