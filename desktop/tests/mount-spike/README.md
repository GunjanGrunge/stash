# Mount spike automated fixture

Run `cargo test --manifest-path desktop/Cargo.toml` for the range-cache tests.
The live mount check requires separately approved WinFsp installation and backend deployment; it must use Explorer and an installed standards-compliant creative app, seek a cloud-only asset, then repeat while network access is disabled. Record observed timing and filesystem error only; never record tokens, object keys, or presigned URLs.
