[package]
name = "stash-desktop"
version = "0.1.0"
description = "STASH Welcome / Sign in desktop shell"
edition = "2021"
rust-version = "1.77"

[build-dependencies]
tauri-build = "2"

[dependencies]
tauri = { version = "2", features = [] }
